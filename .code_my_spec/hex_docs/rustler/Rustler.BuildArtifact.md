# Rustler.BuildArtifact

Represents a single build artifact produced by the Rust build process.

Fields:
  * `:kind` - The type of artifact (`:lib` or `:bin`)
  * `:name` - The name of the artifact
  * `:filename` - The path to the artifact file